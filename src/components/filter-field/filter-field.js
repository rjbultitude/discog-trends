import styled from 'styled-components';
import { padding } from '../../utils/theme';

const FilterFieldStyled = styled.div`
  box-sizing: border-box;
  padding: ${padding.normal} 0 0;
  margin: 0;
`;

export default FilterFieldStyled;
